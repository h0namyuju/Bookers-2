index

<h2>New Book</h2>
<% if @book.errors.any? %>
      <div id="error"><h2><%= @book.errors.count %>errors prohibited this book from being saved:
        </h2>
      <ul>
      <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
      </ul></div>
 <% end %>
 
<%= render 'list', books: @books, book: @book %>
