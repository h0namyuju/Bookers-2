

<table>
 <thead>
   <h2>User info</h2>
    <tr>
       <th>name</th>
       <th>introduction</th>
    </tr>
 </thead>

 <tbody>

   <th><%= user.name %></th>
   <th><%= user.image %></th>

 </tbody>
 </table>
 <%= link_to"編集" %>


<h2>New book</h2>
<%= form_with model: book, url: books_path, method: :post do |f| %>
 <div class="field">
  <%= f.label :Title %>
  <%= f.text_field :title %>
  </div>

  <div class="field">
  <%= f.label :Opinion %>
  <%= f.text_area :body %>
  </div>

  <div class="actions">
    <%= f.submit 'Create Book', class:"btn btn-success" %>
  </div>

<% end %>
