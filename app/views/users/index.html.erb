<p id="notice"></p>

<div class="container">
<div class="row">
 <div class=col-md-3>
    <%= render 'books/list', book: @book, user: @user %>
 </div>


 <div class="col-md-8 offset-md-1">/views/users/index.html.erb</p>

<h1>Users</h1>
<% @books.each do |books| %>
<table>
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td><%= books.user.get_profile_image(80,80) %></td>
      <tb><%=.books.user.name  %></tb>
      <tb><% link_to "Show",user_path(user.id) %></tb>
    </tr>
  </tbody>
</table>



<%= render 'books/list', books: @books, user: @user %>


